My side project is at https://example.com and nobody finds it. Run me through the atlas and
tell me what to fix first.

(Environment: `seo-audit` and `geo-audit` are not installed anywhere — not in `~/.agents/skills/`,
not in `~/.claude/skills/`, not in the project.)
