---
title: Pet Project Atlas
description: 'A route from nobody-finds-it to first traffic: demand, indexing, AI search, content, distribution, analytics, money.'
template: splash
head:
  - tag: title
    content: Pet Project Atlas — a route out of zero traffic
hero:
  title: Pet Project Atlas
  tagline: It has been live a month, and the only visitor in the logs is you.
  actions:
    - text: Start here
      link: /start/
      icon: right-arrow
      variant: primary
    - text: GitHub
      link: https://github.com/Smolevich/pet-project-atlas
      icon: external
      variant: minimal
---

import PathMap from '../../components/PathMap.astro';
import SectionCards from '../../components/SectionCards.astro';

<PathMap />

Read it top to bottom: this is a route out of zero traffic, not a table of contents.

<SectionCards />

## I am not an SEO expert

I build side projects and I read their logs. This is what I saw in my own dashboards, panels and access logs, and how I went looking.

Your data will say something else. The procedure is the part that transfers. My numbers are just what one small project looked like on the day I measured it.

Sometimes the number is embarrassing. One click in three months. A file I told people to publish, fetched zero times by any AI agent. A mysterious crawler in my log that turned out to be my own `curl`.

Those are on the pages too. A route that only shows its wins teaches nobody where the walls are.

## Every page carries three things

A command to run or a check to make, not a principle to consider. Numbers with a date and a source, or no numbers at all. A block called "What did not work", never empty.

Two sections sit outside the route. [Tools](/tools/) says which skill to run for which symptom. [Cases](/cases/) holds the bar a full write-up has to clear, and one case is up: this site, measured on itself.
