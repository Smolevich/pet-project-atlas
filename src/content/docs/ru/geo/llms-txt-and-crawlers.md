---
title: AI-crawler и llms.txt
description: Какие AI-агенты приходят на сайт, кому они принадлежат, где на самом деле решается доступ и что писать в llms.txt.
updated: 2026-08-12
sources:
  - nginx access log, dataset /var/log/nginx/access.log, measured 2026-08-12
  - nginx access log, dataset /var/log/nginx/access.log.*.gz, measured 2026-08-12
  - https://www.rfc-editor.org/rfc/rfc9309.html
  - https://developers.openai.com/api/docs/bots
  - https://support.claude.com/en/articles/8896518-does-anthropic-crawl-data-from-the-web-and-how-can-site-owners-block-the-crawler
  - https://docs.perplexity.ai/guides/bots
  - https://developers.google.com/search/docs/crawling-indexing/google-common-crawlers
  - https://developers.google.com/search/docs/appearance/ai-features
  - https://support.google.com/webmasters/answer/16908024
  - https://blog.google/products-and-platforms/products/search/new-controls-website-owners/
  - https://llmstxt.org/
  - https://darkvisitors.com/
  - https://developers.cloudflare.com/bots/additional-configurations/block-ai-bots/
  - https://developers.cloudflare.com/bots/concepts/bot/
  - https://developers.cloudflare.com/bots/additional-configurations/managed-robots-txt/
  - https://developers.cloudflare.com/ai-crawl-control/features/manage-ai-crawlers/
  - https://developers.cloudflare.com/ai-crawl-control/features/pay-per-crawl/what-is-pay-per-crawl/
  - https://developers.cloudflare.com/ruleset-engine/rules-language/actions/
  - https://developers.cloudflare.com/waf/analytics/security-events/
---

## Что решаем

Надо решить, кому разрешено читать сайт, и решается это обычно случайно. Заблокированный crawler — это отсутствие в AI-ответах при любом качестве контента.

Ловушка в том, что «AI-бот» — не одна сущность. За этими словами прячутся три разные работы, и отказ каждой из них стоит по-разному.

## Шаги

Сначала разложить агентов по работе, потом что-то решать. Поисковый crawler, обучающий crawler и загрузчик, действующий по просьбе пользователя, — три разных решения.

| User agent | Владелец | Работа | Чего стоит блокировка |
|---|---|---|---|
| `OAI-SearchBot` | OpenAI | Поисковый индекс | Вас нет в результатах поиска ChatGPT |
| `ChatGPT-User` | OpenAI | Загрузка по просьбе пользователя | Почти ничего — OpenAI пишет, что robots.txt здесь может не применяться |
| `GPTBot` | OpenAI | Обучающий корпус | В сегодняшних ответах — ничего |
| `Claude-SearchBot` | Anthropic | Поисковый индекс | Вас нет в веб-результатах Claude |
| `Claude-User` | Anthropic | Загрузка по просьбе пользователя | URL, который вставит ваш читатель, в чате не откроется |
| `ClaudeBot` | Anthropic | Обучающий корпус | В сегодняшних ответах — ничего |
| `PerplexityBot` | Perplexity | Поисковый индекс | Вас нет в движке с самой ясной атрибуцией |
| `Perplexity-User` | Perplexity | Загрузка по просьбе пользователя | Почти ничего — Perplexity пишет, что обычно игнорирует robots.txt |
| `Google-Extended` | Google | Управляющий токен | Вне обучения Gemini и grounding в Vertex AI |
| `GoogleOther` | Google | Разные внутренние загрузки | В AI-ответах — почти ничего |
| `CCBot` | Common Crawl | Открытый корпус для обучения | В сегодняшних ответах — ничего |

1. **По умолчанию пускать всех** — весь файл для большинства проектов это четыре строки.

   ```text
   User-agent: *
   Allow: /

   Sitemap: https://example.com/sitemap-index.xml
   ```

   Именно это отдаёт данный сайт. Имя агента писать только тогда, когда его надо закрыть.

   CDN умеет переписать этот файл, так что заберите его с боевого домена и прочитайте, что отдаётся. Managed-версия Cloudflare живёт на странице **Security Settings** зоны, под фильтром **Bot traffic**. Она есть на всех тарифах, и включённая дописывает свои строки `Disallow` перед вашими.

   Домену на Free-тарифе без собственного файла вместо него отдают Content Signals Policy от Cloudflare.

   Список агентов, разрешённых поимённо, — это список, который надо поддерживать. Вендоры выпускают новые имена быстрее, чем кто-либо перечитывает файл.
   Поисковые и вызываемые пользователем агенты решают, существуете ли вы в сегодняшнем ответе. Обучающая группа — вопрос ценностей, и только ради неё имя стоит набирать.
2. **Понимать, какие ваши правила вообще обязывают** — `robots.txt` соблюдают crawler, а загрузчики не всегда.
   OpenAI пишет: раз действие инициировал пользователь, правила robots.txt могут не применяться. Perplexity говорит, что её user agent обычно этот файл игнорирует, по той же причине.
   Anthropic — исключение: она пишет, что её боты файл соблюдают. Один `Disallow`, три вендора, три разных исхода.
3. **Понимать, чем `Google-Extended` не является** — это управляющий токен без собственного user agent, и на ранжирование он не влияет.
   Он покрывает обучение моделей Gemini и Grounding with Google Search в Vertex AI. В Google Search его блокировка не меняет ничего.
   AI Overviews — часть Search, и обходит для них `Googlebot`.
4. **Из AI Overviews выходить отдельным контролом** — Search Console, Settings, Search generative AI.
   Он покрывает AI Overviews, AI Mode и генеративные блоки в Discover. Google пишет, что сигналом ранжирования в остальном Search он не служит.
   `nosnippet`, `data-nosnippet` и `max-snippet` тоже выводят — и забирают с собой обычный сниппет в выдаче. Отдельный контрол такой цены не берёт.
   Раскатывают его пока на часть владельцев, так что в вашем ресурсе его может ещё не быть.
5. **Если всё-таки называете агентов — от частного к общему** — так написано в RFC 9309.
   Crawler слушается той группы, чьё имя совпало с его точнее всего. Щедрый `User-agent: *` не спасёт агента, которого вы заблокировали выше.
   Правило работает в обе стороны. Одна протухшая группа молча перебивает wildcard под собой, поэтому поимённого списка разрешений лучше не заводить.
6. **Проверять слои под `robots.txt`** — заголовок `X-Robots-Tag`, мета-тег и edge.
   Переключатель в CDN, правило WAF — firewall, который матчит HTTP-запросы, — или rate limit отвечает 403. И ни один не виден в файле, который вы правите.

   В Cloudflare этот контрол называется **Configure AI bot policies** и лежит на странице **Security Settings** зоны. Он есть на всех тарифах, включая Free, и раскладывает агентов на Search, Agent и Training. Для каждого из трёх доступно Allow, Block on all pages или Block only on pages with ads.

   Каких агентов покрывают эти три слова — список Cloudflare, и он меняется без вас. Block терминирующий: Cloudflare отвечает 403 из своей сети, и ваш сервер запроса не видит. С 15 сентября 2026 новый для Cloudflare домен приезжает с блоком Training и Agent на страницах с рекламой.

   Старый одиночный переключатель рядом, **Block AI bots**, в тот же день уходит. Существующие зоны сохраняют свои настройки, и отказаться от новых умолчаний можно до этой даты. Та же панель умеет не отказывать, а брать деньги: pay per crawl отвечает 402 и живёт в закрытой бете.
7. **Проверять личность по сети, а не по имени** — строка user agent это заявление, и отправить её может кто угодно.
   Вендоры публикуют диапазоны IP и обратный DNS для своих crawler. Сверяйтесь с ними, прежде чем считать строку лога доказательством визита или атаки.
8. **Публиковать `llms.txt` только при документации для разработчиков** — файл стоит десять минут.
   Читать его никто из вендоров не обязался.
   Общий случай закрывает мой собственный лог. За 16 дней ни один AI-агент не забрал файл ни разу: [кто к вам на самом деле ходит](/ru/geo/who-actually-crawls-you/).
   То есть на маленьком сайте про продукт это дешёвая ставка с измеренной отдачей 0. В документации на сорок страниц — разумная.
   В остальных случаях пропустить, а десять минут потратить на `robots.txt` и sitemap, которые забирают все.

   Формат, если всё-таки пишете, — markdown-файл, который называет страницы, которые стоит прочитать.
   `robots.txt` говорит, куда не ходить. `llms.txt` говорит, что важно, вашими словами.

   ```markdown
   # Название проекта

   > Одно предложение о том, что это и для кого.

   ## Docs

   - [Заголовок страницы](https://example.com/page): о чём она и когда пригодится.

   ## Optional

   - [Второстепенная страница](https://example.com/extra): можно пропустить при жёстком лимите контекста.
   ```

   Правил немного. H1 первой строкой, под ним цитата в одно предложение, абсолютные ссылки, описание после каждого двоеточия.
9. **Прогрепать access-лог и увидеть, кто приходил на самом деле** — имена агентов выше плюс запросы к `/llms.txt`.
   Поддержка этого файла — договорённость, а не стандарт, который кто-то принуждает соблюдать. Честный ответ о том, кто его читает, есть только в вашем логе.
   Мой посчитан здесь: [кто к вам на самом деле ходит](/ru/geo/who-actually-crawls-you/).

   Запрос, который заблокировал edge, до вашего сервера не доходит и строкой в этом логе не становится. Грепните агента, которому Cloudflare отказывает, и не найдёте ничего. Лог тогда читается как мир, в который никто не приходил, и это неверный вывод.

   Эти запросы считаются по ту сторону блокировки. Cloudflare считает их в **AI Crawl Control**, во вкладках **Crawlers** и **Metrics**. Сама блокировка ложится в **Analytics** → **Events**, и на Free-тарифе хранится 24 часа.

## Что не сработало

- **Закрыть всех, чтобы сэкономить трафик**. Блокировка стояла на edge, в собственной AI-настройке CDN, а не в `robots.txt`. Agent — одно из трёх поведений, которые эти настройки покрывают, поэтому остановились и загрузчики. Читателю, вставившему URL в чат, ответили, что страницу открыть нельзя.
- **Потерять цитирование вместе со счётом**. Самый активный AI-агент в моём логе сделал 341 запрос за 16 дней — [кто к вам на самом деле ходит](/ru/geo/who-actually-crawls-you/). Это `ChatGPT-User`, запускает его человек, и что этим человеком был не я, я доказать не могу. Присутствие в ответе всё равно дёшево не возвращается.
- **Сделать `llms.txt` обязательным пунктом**. Эта страница велела его публиковать, маршрут повторял указание, а мой лог за 16 дней записал 0 обращений от AI-агентов. Файл, который никто не читает, не пункт чек-листа. Теперь он условный, и условие — документация для разработчиков.
- **Разрешать агентов в `robots.txt` поимённо**. Маршрут просил читателя перечислить каждого поискового и вызываемого пользователем агента. Сам сайт так не делал никогда: он отдаёт `User-agent: *` и `Allow: /`. Поимённый список — работа с истекающим сроком, а RFC 9309 делает так, что протухшая группа перебивает wildcard под собой.
- **Хвататься за `Google-Extended`, чтобы управлять AI Overviews**. Не тот рычаг: AI Overviews работает на `Googlebot`, а токен покрывает обучение Gemini и grounding в Vertex AI. Правило не сдвинуло ничего, а контрол, который AI Overviews правда покрывает, теперь живёт в Search Console.
- **Выходить из AI Overviews через `nosnippet`**. Так на этой странице и было написано — до 3 июня 2026, когда Google выкатил отдельный контрол в Search Console. Директивы сниппета вдобавок отбирают обычный сниппет в выдаче, а отдельный контрол такой цены не берёт.
- **Верить странице вендора, которая перестала обновляться**. В документации Google `ai-features` до сих пор перечислены только директивы сниппета. Обновлена она 2025-12-10, так что эта страница честно повторяла устаревший источник.
- **Написать `Disallow` для `ChatGPT-User` и считать это блокировкой**. OpenAI пишет, что загрузка по просьбе пользователя может игнорировать robots.txt, Perplexity — то же самое про свою. Правило было просьбой к клиенту, которому уже сказали, что слушаться не обязательно.
- **Править `robots.txt`, пока блокировал edge**. Файл разрешал каждого агента поимённо. Правило bot-protection перед ним отдавало 403, и в логе не было ни одной успешной загрузки. Это правило не видно ни в `robots.txt`, ни в исходнике страницы, ни в логе сервера. Видно его только в security events самого CDN.
- **Перечислять в `llms.txt` планы**. Строка в этом файле — обещание, что страница за ней написана. Недоделанные ссылки учат читателя — человека или модель — этот файл игнорировать.
- **Считать `robots.txt` запретом**. Это просьба, которую воспитанный crawler выполняет. Скрапер, который её игнорирует, останавливается на edge или не останавливается вообще.

## Проверить

Запустить `geo-crawlers` из раздела [Tools](/ru/tools/). Он читает `robots.txt`, мета-теги и заголовки ответа. На выходе — карта доступа по агентам, и так ловится забытое правило.

Оттуда же запустить `geo-llmstxt`. Он проверяет существующий файл или собирает черновик по структуре сайта и помечает ссылки, ведущие в никуда.

Потом подтвердить снаружи:

- Открыть **Security Settings** в Cloudflare и прочитать, что стоит в **Configure AI bot policies**. Умолчание, которого вы не выбирали, — всё равно ваша настройка.
- Открыть **Analytics** → **Events** и отфильтровать действие по Block. Агентов, которым отказали там, нет ни в одном логе на вашей машине.
- Запросить страницу с user agent какого-нибудь crawler из чужой сети и убедиться, что пришло 200.
- Если `llms.txt` опубликован — забрать его так же и проверить, что каждая ссылка в нём отдаёт 200.
- Попросить ассистента открыть один из ваших URL. Если не может — блокировка реальна, а диагностика выше говорит, на каком слое она стоит.
- Записать минуту, в которую вы это сделали. В логе она ляжет как `ChatGPT-User` или `Claude-User`, с адреса вендора, и от визита постороннего её не отличить.

Доступ — это только ворота. Что процитируют после них — отдельная задача: [почему AI цитирует не вас](/ru/geo/citable-pages/).
